const foo = 0;
const bar = 0;

const mydivision = foo / bar 

console.log( mydivision );
// Expected output NaN

function buttonlogclick() {
    // The outcome will be NaN in this case
    if ( isNaN(mydivision)) {
        alert("You can't divide us");

    } 

console.log( "Divide" )

}
